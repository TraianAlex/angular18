export type UserData = {
  id: number;
  name: string;
  email: string;
};

export type User = {
  readonly name: string;
  readonly age: number;
};

export type Product = {
  id: number;
  name: string;
  category: string;
};
